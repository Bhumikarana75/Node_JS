<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit User_Info</title>
    <link rel="stylesheet" href="css/edit.ejs">
</head>
<body>

    <div class="container">
        <header class="header">
            <h1>Edit User</h1>
        </header>
        <section class="form-section">
            <form action="/editData" method="post" class="user-form">
                <input type="hidden" name="editId" value="<%= singleData.id %>">
                <div class="form-group">
                    <label for="username">Name:</label>
                    <input type="text" id="username" name="username" value="<%= singleData.username %>" required>
                </div>
                <div class="form-group">
                    <label for="usertask">Task:</label>
                    <input type="text" id="usertask" name="usertask" value="<%= singleData.usertask %>" required>
                </div>
                <div class="form-group">
                    <label for="userstatus">Status:</label>
                    <select id="userstatus" name="userstatus" required>
                        <option value="pending" <%= singleData.userstatus === 'pending' ? 'selected' : '' %>>Pending</option>
                        <option value="ongoing" <%= singleData.userstatus === 'ongoing' ? 'selected' : '' %>>Ongoing</option>
                        <option value="completed" <%= singleData.userstatus === 'completed' ? 'selected' : '' %>>Completed</option>
                    </select>
                </div>
                <div class="form-group">
                    <label for="userdeadline">Deadline:</label>
                    <input type="datetime-local" id="userdeadline" name="userdeadline" value="<%= singleData.userdeadline %>" required>
                </div>
                <div class="form-buttons">
                    <input type="submit" class="btn btn-submit" value="Update User">
                    <a href="/" class="btn btn-cancel">Cancel</a>
                </div>
            </form>
        </section>
    </div>
    
</body>
</html>